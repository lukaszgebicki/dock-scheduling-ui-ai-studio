# Decision log

Stable IDs do not encode dates. `ACCEPTED` decisions remain authoritative
until replaced by a later decision that cites the superseded ID.

| ID | Status | Decision | Rationale | Consequences |
| --- | --- | --- | --- | --- |
| DCF-001 | ACCEPTED | Keep the UI sandbox separate from the production repository. | Sandbox experiments must not create implicit production authority. | Production access and automatic transfer are prohibited. |
| DCF-002 | ACCEPTED | Keep the sandbox frontend-only. | The verified repository contains a React UI demonstration, not production services. | Backend, database, persistence, and deployment work require separately approved architecture and scope. |
| DCF-003 | ACCEPTED | Perform task writes only in external worktrees. | Canonical `main` must remain a stable baseline. | Nested and in-repository worktrees are prohibited. |
| DCF-004 | ACCEPTED | Use one task, one branch, and one implementation owner. | Scope, evidence, and accountability must remain attributable. | Do not combine tasks or edit another active task's worktree. |
| DCF-005 | ACCEPTED | Separate Builder and Reviewer roles. | Self-review cannot provide independent assurance. | A separate read-only Reviewer issues the independent verdict. |
| DCF-006 | ACCEPTED — PARTIAL | Centralize new access-scope and administration data in the typed demo access-scope source. | Stable warehouse IDs, supplier-organization IDs, and assignments are shared domain facts. | New work uses `src/users/demoAccessScope.ts`; legacy label and access duplication remains in `src/users/demoUsers.ts` and is tracked as `DEMO-DATA-001`. |
| DCF-007 | ACCEPTED | Keep demo success behavior local-only. | The sandbox has no persistence contract. | Success states must say what was not created, sent, or saved. |
| DCF-008 | ACCEPTED | Do not fake persistence. | A UI demonstration must not imply durable effects. | No catalogue mutation, storage write, or invented pending state without approved persistence scope. |
| DCF-009 | ACCEPTED | Use stable internal IDs and resolve display names centrally. | Names can change and are not safe relational identifiers. | Forms, schemas, and assignments use stable IDs. |
| DCF-010 | ACCEPTED | Require human gates for business and architecture decisions. | Repository evidence cannot resolve product intent or architectural authority. | Codex stops on material ambiguity. |
| DCF-011 | ACCEPTED | Keep merge human-controlled. | Merge is the final product and repository authority boundary. | Automation, CI, and Reviewer PASS cannot merge. |
| DCF-012 | ACCEPTED | Bound autonomous delivery at an open pull request. | This permits useful execution while preserving human control. | Codex stops at a verified PR and waits for merge authority. |
| DCF-013 | ACCEPTED | Require minimal diffs and repository fit. | Small, conventional changes are easier to review and maintain. | Unrelated cleanup and unjustified new patterns fail review. |
| DCF-014 | ACCEPTED | Require an Engineering Quality Score of at least 8/10. | Passing commands alone does not establish human-grade quality. | No category may be 0 and every 0 or 1 requires justification. |
| DCF-015 | ACCEPTED | Track accepted technical debt explicitly. | Silent debt creates false assurance and loses remediation context. | Verified debt receives a stable ID, exposure statement, owner gate, and follow-up task. |
| DCF-016 | ACCEPTED | Remediate the React Router runtime audit finding in isolated Class C task `SPR-SEC-2`. | The preserved `AUTONOMY-GOV-1` implementation could not pass its mandatory runtime audit, while stale post-merge roadmap state created a bootstrap deadlock. | The runner was preserved and paused until remediation passed. Audit suppression, overrides, `npm audit fix`, force, and legacy-peer behavior were prohibited. Lucide React could move only from 0.394.0 to 0.397.0 to satisfy the approved React 19 peer contract. The one-time recovery amendment resolved only this stale-roadmap deadlock and creates no precedent for bypassing `READY`. |
| DCF-017 | ACCEPTED | Use a repository-native local Node.js runner for bounded task orchestration. | Safety gates must be enforced by deterministic code around separate Codex processes rather than entrusted only to prompts. | The runner accepts strict local or GitHub issue contracts, stores locks and run evidence outside the repository, constrains Reviewers to read-only access, permits publication only after verified PASS, and contains no merge path. It is not active until reviewed, merged, and successfully piloted. |
| DCF-018 | ACCEPTED | Adopt Dock AI Playbook v2 with separate risk-class and E0-E4 execution dimensions. | Model routing, reasoning, context and token economy must be auditable without allowing task text to control raw runtime arguments. | Contracts select only trusted policy IDs; installed capabilities are verified before agents run; E2 and E3 cannot publish, E4 stops before merge, and execution level never reduces risk or Class C authorization. |
| DCF-019 | ACCEPTED | Treat the complete Business Decision Pack UI MVP v0.2 as the approved product authority for UI MVP onboarding and later contracted UI work. | Łukasz Gębicki explicitly approved the supplied version 0.2 on 2026-07-28. | Codex may implement only BDP or AC identifiers named by an active contract and must stop for a human decision on any conflict or ambiguity. |
| DCF-020 | ACCEPTED | Interpret every `REKOMENDACJA DO ZATWIERDZENIA` marker in Business Decision Pack UI MVP v0.2 as approved by Łukasz Gębicki on 2026-07-28; section 24 remains explicitly excluded from UI MVP. | The approval makes the recommendation markers authoritative without silently changing the source decisions or expanding excluded scope. | Canonical onboarding must preserve the source decisions and section 24 exclusions. No excluded item may be implemented without a separate accepted decision, and Codex must stop on conflict or ambiguity. |
| DCF-021 | ACCEPTED | Treat the canonical Business Decision Pack UI MVP v0.2, traceability matrix, and implementation plan as authoritative product-planning sources; only an active exact-SHA task contract grants implementation permission. | Canonical planning artifacts preserve approved scope, coverage, and sequencing without converting roadmap traceability into execution authority. | Every implementation task must be separately `READY` and contracted at an exact `main` SHA. No planned task becomes `READY` through this decision, and Codex must stop on conflict or ambiguity. |
| DCF-022 | ACCEPTED | Make Business Decision Pack UI MVP v0.3 the canonical product source and retain v0.2 unchanged as historical evidence. | The approved weekly-planning package and `BDR-TRN-001` qualify earlier general booking and transport assumptions. | Sections 29–30 control weekly-planning conflicts; section 24 remains excluded, and canonical planning still grants no implementation authority. |
| DCF-023 | ACCEPTED | Restrict the initial Supplier weekly-planning input to one Supplier, warehouse, week, PO and slot with fixed delivery part `"1"`, no SKU entry or split UI, and both transport registration fields mandatory. | Supplier reserves W+1 capacity in week W before Friday PO/SKU enrichment, while `BDR-TRN-001` requires complete transport identity at reservation time. | The general five-step booking and later vehicle completion rules do not apply to this flow; Administrator transport changes are explicit and auditable. |
| DCF-024 | ACCEPTED | Reserve Friday delivery-details import for System Administrator globally and assigned Warehouse Administrator within assigned warehouses. | Import is an administrative enrichment and reconciliation operation, not a Supplier or default Warehouse Operator action. | Missing authorized participation yields an explicit delegation or block; local UI results never imply persistence or ERP/WMS/SAP integration. |
| DCF-025 | ACCEPTED | Represent weekly-planning delivery data as a PO header with zero-to-many SKU lines. | Supplier reservation precedes SKU detail and exact Friday enrichment attaches lines to the matching PO. | Initial UI does not split PO deliveries; `deliveryPartKey` remains `"1"`, and booking origin and planning status stay explicit. |
| DCF-026 | ACCEPTED | Limit the `warehouse + loadCarrierType + goodsCategory` transport matrix to downstream readiness and Administrator-added or imported deliveries. | `BDR-TRN-001` independently requires both transport fields for every Supplier reservation. | Matrix combinations cannot remove Supplier fields, silently overwrite them, move a slot or bypass explicit conflict reconciliation. |
| DCF-027 | ACCEPTED | Provide role-scoped reports at PO and SKU level with planned dates. | Weekly all-delivery and monthly Slipsheet views require the enriched planning model. | CSV/XLSX export remains local and demonstrational; advanced reporting warehouse and integrations stay excluded. |
| DCF-028 | ACCEPTED | Route workflow participation by approved capabilities and explicit `RUN`, `SKIP`, `DELEGATE` or `BLOCK` outcomes across the six existing global role types. | Roles may be optional in a workflow without inventing placeholder actors or new role types. | Missing Supplier uses `SKIP` and the Admin path; mandatory absence uses only authorized delegation or block, with no new data access or lifecycle transition. |
